{
  "name": "phonebook_backend",
  "version": "0.0.1",
  "description": "Backend for full stack open phonebook web app",
  "license": "MIT",
  "author": "Johnny Varila",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../phonebook_frontend && npm install . && npm run build && cp -r dist ../phonebook_backend/"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.7.4",
    "morgan": "^1.11.0"
  }
}
