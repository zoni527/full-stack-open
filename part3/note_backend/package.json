{
  "name": "note_backend",
  "version": "0.0.1",
  "description": "Backend for full stack open notes web app",
  "license": "MIT",
  "author": "Johnny Varila",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../note_frontend && npm run build && cp -r dist ../note_backend/"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
